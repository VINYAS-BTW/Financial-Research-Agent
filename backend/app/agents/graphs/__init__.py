"""
Nodes package for individual agent steps (fetch, sentiment, indicators, llm, etc).

Do NOT import node functions here to avoid circular imports.
Always import directly from the concrete modules, e.g.:

    from app.agents.nodes.fetch_node import fetch_research_data
"""
